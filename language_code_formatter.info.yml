name: Language Code Formatter
type: module
description: 'Provide a formatter for the language field type to output the internal language code.'
core_version_requirement: ^8 || ^9
dependencies:
  - node
